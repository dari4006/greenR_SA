function getDistance(x1, y1, x2, y2){
    let y = x2 - x1;
    let x = y2 - y1;
    calcRooted = Math.sqrt(x * x + y * y)
    distanceCalculated = calcRooted * 100
}

//function renderDitanceCalculation(coords1, coords2){
    //coords1 = 
//}


function renderCoordinates(c1, c2){
    let c1 = new1[0]
    let c2 = new1[1]
    console.log(c1, c2)
}